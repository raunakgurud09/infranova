#!/bin/sh
# No longer use `husky.sh` bootstrap

echo "🔍 Running frontend lint..."
cd spanfront && npm run lint

echo "🧪 Running backend tests..."
cd ../corenode && npm test

echo "✅ Pre-commit checks passed."
